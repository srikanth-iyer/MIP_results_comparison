Output folder for reformatted data. 

To reformat the genx results data, place the renx results in the genx_scenarios_results folder. 

Then,

```python
python reformat_data_for_plot/format_genx_result_for_plotting.py
```

(default input: (genx_scenarios_results); default output:(genx-scenarios))
